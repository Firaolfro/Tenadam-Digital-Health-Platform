package events

// Event types for organization-service.
const (
EventCreated = "organization-service.created"
EventUpdated = "organization-service.updated"
EventDeleted = "organization-service.deleted"
)
