package repository

import (
	"context"
	"github.com/tenadam/revenue-cycle-service/internal/model"
)

// ListRevenueCycles retrieves all revenue-cycle records.
func (r *Repository) ListRevenueCycles(ctx context.Context) ([]*model.RevenueCycle, error) {
	return nil, nil
}
