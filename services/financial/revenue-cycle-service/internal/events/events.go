package events

// Event types for revenue-cycle-service.
const (
EventCreated = "revenue-cycle-service.created"
EventUpdated = "revenue-cycle-service.updated"
EventDeleted = "revenue-cycle-service.deleted"
)
